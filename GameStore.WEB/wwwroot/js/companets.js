function InitializationMultiSelect2(conteiner) {
    $('.' + conteiner).select2({
        closeOnSelect: false,
    });
}

function InitializationSelect2(conteiner) {
    $('.' + conteiner).select2({
        closeOnSelect: true,
    });
}