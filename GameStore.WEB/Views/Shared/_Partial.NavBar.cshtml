@{
    string method = ViewContext.RouteData.Values["action"].ToString();
    bool isIndex = (method.Contains("Index"));
    bool isCatalog = (method.Contains("Catalog"));
    bool isDiscounts = (method.Contains("Discounts"));
    bool isSupport = (method.Contains("Support"));

}

<nav class="navbar navbar-dark navbar-expand-lg   m-2 h4">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                <!-- Добавляем класс me-auto для выравнивания -->
                <a class="nav-link @((isIndex) ? "active" : "")" asp-area="" asp-controller="Home" asp-action="Index">Главная</a>
                <a class="nav-link @((isCatalog) ? "active" : "")" asp-area="" asp-controller="Catalog" asp-action="Catalog">Каталог</a>
                <a class="nav-link @((isSupport) ? "active" : "")" asp-area="" asp-controller="Home" asp-action="Support">Поддержка</a>
            </div>
             @* <form class="d-flex">
                 <input class="form-control w-auto" type="search" placeholder="Найти игру (Функция в разработке...)" aria-label="Search">
             </form> *@
        </div>
    </div>
</nav>